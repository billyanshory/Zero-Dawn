1. **Fix Laporan Qurban Bug (Bug 1)**: Change the form submit to use `fetch` avoiding hard reload, and update the `/admin/qurban/stats` route to accept JSON and return JSON (with try/except blocks), fixing the 500 server error and updating frontend correctly.
2. **Fix Daftar Shohibul Bug & Mechanism (Bug 2)**: Fix `/qurban/lacak` to handle exceptions safely, create admin-only PIN mechanism UI with JS conditionally rendered via `is_admin`, add `/api/qurban/shohibul/generate_pin` to safely generate PINs using `secrets` and save in DB, and `/api/qurban/shohibul/list_pins` to retrieve them safely.
3. **Fix Pembagian Bug & Mechanism (Bug 3)**: Fix `/qurban/pembagian/cek` route POST handler to properly find items and return data without crashing. Build admin mechanism to generate `Nomor Kupon (2FA)` with `secrets` module, store it, handle uniqueness and listing of Kupon inside UI for admins securely.
4. **Fix Peta Distribusi Bug (Bug 4)**: Added try/except block around the `/admin/qurban/peta` route to return safe 500 error messages to prevent raw Flask 500 on unhandled Python exception, and ensure there are no unescaped JS `{}` braces causing KeyError inside the HTML templates that use `.format` or `f-strings` (or Jinja). Fixed syntax and evaluated successfully.
5. **Restore Config & Pre-commit Steps**: Restored `SQLALCHEMY_DATABASE_URI` and execution command to monolithic production specs, running python compilation syntax checks. Will get pre commit instructions and submit.
